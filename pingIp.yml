---
- name: Get IP and Hostname
  hosts: all
  gather_facts: yes  
  tasks:
    - name: Create ACL to block IPs
      ios_config:
        lines:
          - "access-list standard Block_IP_ACL"
          - "deny host 192.168.40.100"

